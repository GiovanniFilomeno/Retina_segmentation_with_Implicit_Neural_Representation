# Runtime dependencies for environments that do not install from pyproject.toml.
# Prefer `python -m pip install -e .` for an editable package installation.
albumentations>=1.4,<3
numpy>=1.24,<3
opencv-python-headless>=4.8,<5
PyYAML>=6,<7
torch>=2.2,<3
